# Bulk

Move many records with one payload.

Common uses:

- `monk bulk run`
- `monk bulk export`
- `monk bulk import`
- `monk bulk create users`

Use `--help` on a subcommand for the next level down.
