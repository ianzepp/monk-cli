# Fs

Read, write, and delete tenant files.

Use this branch for tenant-scoped filesystem operations instead of model data.

Common uses:

- `monk fs get /path/to/file`
- `monk fs put /path/to/file`

Use `--help` on a subcommand for the next level down.
