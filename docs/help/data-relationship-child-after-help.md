# Data Relationship Child

Work with one nested child record.

Use this when a relationship has a specific child id and you need to address it
surgically instead of acting on the whole collection.

This branch is intentionally concise; use the command itself for the full flag list.
