# Trashed

Review and restore soft-deleted records.

Use this branch when you need to inspect deleted records or bring them back.

Common uses:

- `monk trashed list`
- `monk trashed record users 123`

Use `--help` on a subcommand for the next level down.
