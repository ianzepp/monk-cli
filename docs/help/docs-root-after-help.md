# Docs Root

Open the API discovery page.

Good follow-up targets:

- `/docs/auth`
- `/docs/api/data`
- `/docs/api/describe`
- `/docs/api/find`
- `/docs/api/aggregate`
- `/docs/api/bulk`

This branch is intentionally concise; use the command itself for the full flag list.
