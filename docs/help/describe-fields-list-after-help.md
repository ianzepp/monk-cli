# Describe Fields List

List the fields for one model.

This is the quickest way to inspect how a model is structured before editing a
specific field.

This branch is intentionally concise; use the command itself for the full flag list.
