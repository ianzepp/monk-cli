# Data Relationship

Work with owned child collections under a parent record.

This is the branch for relationship-scoped routes like
`/api/data/:model/:id/:relationship`.

This branch is intentionally concise; use the command itself for the full flag list.
