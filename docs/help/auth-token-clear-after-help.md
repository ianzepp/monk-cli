# Auth Token Clear

Remove the saved JWT from local config.

Use this when you want to force the CLI to stop reusing the current session
state.

This branch is intentionally concise; use the command itself for the full flag list.
