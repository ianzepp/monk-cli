# Docs Path

Fetch a docs page by path.

Useful paths include:

- `/docs`
- `/docs/auth`
- `/docs/api/data`
- `/docs/api/describe`
- `/docs/api/find`
- `/docs/api/cron`

This branch is intentionally concise; use the command itself for the full flag list.
