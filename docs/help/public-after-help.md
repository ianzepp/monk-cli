# Public

Open the API discovery pages that are safe to read without auth.

Good starting points:

- `monk public root` for the human-facing discovery page
- `monk public llms` for the agent-facing markdown entrypoint

Use `--help` on `root` or `llms` for the next level down.
