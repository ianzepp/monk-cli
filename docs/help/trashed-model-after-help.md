# Trashed Model

List soft-deleted records for one model.

Use this when you need the deleted items for a specific model only.

This branch is intentionally concise; use the command itself for the full flag list.
