# Tracked

Inspect tracked changes for a record.

Use this branch when you want to see the change log for one record or drill into
a specific tracked change.

Common uses:

- `monk tracked list users 123`
- `monk tracked get users 123 change-id`

Use `--help` on a subcommand for the next level down.
