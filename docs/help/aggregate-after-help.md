# Aggregate

Request aggregate calculations for a model.

Common uses:

- `monk aggregate get users`
- `monk aggregate run users --count`
- `monk aggregate run users --sum balance`

Use `--help` on `get` or `run` for the next level down.
