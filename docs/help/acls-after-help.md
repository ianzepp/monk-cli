# Acls

Inspect and edit record access control lists.

Use this branch when you need to read or modify record-level permissions for a
specific model and id.

Common uses:

- `monk acls get users 123`
- `monk acls update users 123`

Use `--help` on a subcommand for the next level down.
