# Auth Token Set

Overwrite the saved JWT in local config.

Use this when you already have a token and want the CLI to use it on future
requests without logging in again.

This branch is intentionally concise; use the command itself for the full flag list.
