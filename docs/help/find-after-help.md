# Find

Run filtered and saved queries over records.

Common uses:

- `monk find query users --where ...`
- `monk find saved users recent`
- `monk find query users --select id,name --limit 10`

Use `--help` on `query` or `saved` for the next level down.
