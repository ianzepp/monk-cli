# Auth Token Get

Print the saved JWT.

This is useful when you need to inspect the current local session token for
manual debugging or handoff.

This branch is intentionally concise; use the command itself for the full flag list.
