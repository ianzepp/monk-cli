# Stat

Read record metadata and status.

This branch exposes lightweight metadata like timestamps and other record
summary values.

Common uses:

- `monk stat get users 123`

Use `--help` on a subcommand for the next level down.
