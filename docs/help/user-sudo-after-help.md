# User Sudo

Request elevated sudo access for the current user.

Use this when you need administrative actions that require an explicit reason.

This branch is intentionally concise; use the command itself for the full flag list.
